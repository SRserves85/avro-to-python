{%- macro get_json_schema(schema) -%}
{{'schema = """\n' + json.dumps(schema, indent=4) + '\n"""'}}
{%- endmacro -%}

{%- block schema -%}
{{get_json_schema(file.get('schema'))|indent(4)}}
{% endblock -%}
