{%- macro get_json_schema(schema) -%}
{{'schema = r"""\n' + json.dumps(schema, indent=4) + '\n"""'}}
{%- endmacro -%}

{%- block schema -%}
{{get_json_schema(file.orig_schema)|indent(4)}}
{% endblock -%}
